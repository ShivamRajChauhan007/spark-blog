// Minimal flat config — Next 16 dropped the bundled `next lint`. We rely on
// `tsc --noEmit` for type checking; this file exists so editors that auto-detect
// eslint don't error out on unknown rules.
export default [];
